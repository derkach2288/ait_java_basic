package abstract_class;

public interface House {
    // принять на довольство
    boolean addPet(Pet pet);

    // снять с довольства
    Pet removePet(Pet pet);

    // купить всем еду

    // гигиена
}
